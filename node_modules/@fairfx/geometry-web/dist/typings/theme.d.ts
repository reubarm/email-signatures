import { flatColorPalette } from '@fairfx/geometry-palette';
import { breakpoints, sizes, spaces, transitions, zIndices } from '../theme/tokens';
export declare type Spaces = keyof typeof spaces;
export declare type Sizes = keyof typeof sizes;
export declare type Colors = keyof typeof flatColorPalette;
export declare type ZIndices = keyof typeof zIndices;
export declare type Breakpoints = keyof typeof breakpoints;
export declare type Transitions = keyof typeof transitions;
