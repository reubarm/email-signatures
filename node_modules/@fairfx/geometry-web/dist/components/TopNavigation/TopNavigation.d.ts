import React from 'react';
export interface Route<UrlProps> {
    name: string;
    url: (props: UrlProps) => string;
    comparator: (path: string) => boolean;
}
declare type TopNavigationProps<UrlProps> = {
    routes: Route<UrlProps>[];
    currentPath: string;
    urlProps: UrlProps;
};
declare type TopNavigationType = <UrlProps>(props: TopNavigationProps<UrlProps>) => React.ReactElement;
declare const TopNavigation: TopNavigationType;
export default TopNavigation;
