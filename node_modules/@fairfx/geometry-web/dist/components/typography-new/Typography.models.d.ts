import { Theme } from '../../theme/models';
import { TypographyColorContext } from '../../theme/models.base';
import { spaces } from '../../theme/tokens';
import { FontWeights } from '../../themer/base/typography';
export interface TypographyProps extends BaseTypographyProps {
    size?: 'xxs' | 'xs' | 'sm' | 'md' | 'lg';
}
export declare type Headinglevels = 1 | 2 | 3 | 4;
export interface TypographyPropsHeading extends BaseTypographyProps {
    variant?: Headinglevels;
    visualLevel?: Headinglevels;
}
interface BaseTypographyProps {
    weight?: keyof FontWeights;
    align?: 'left' | 'center' | 'right';
    color?: keyof TypographyColorContext;
    theme?: Theme;
    marginBottom?: keyof typeof spaces;
    ['data-testid']?: string;
}
export {};
