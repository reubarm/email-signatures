import { Color, FontSize, FontWeight } from '../../themer/types';
export declare type SupportedAsKey = 'p' | 'h1' | 'h2' | 'h3' | 'h4' | 'label' | 'div' | 'span';
export declare type SupportedStyleAsKey = SupportedAsKey | 'body' | 'cta' | 'placeholder' | 'small' | 'large';
declare type TextAlign = 'left' | 'center' | 'right';
export interface Style {
    textAlign: TextAlign;
    fontWeight: FontWeight;
    fontSize: FontSize;
    color: Color;
}
export interface TextProps {
    as?: SupportedAsKey;
    styleAs?: SupportedStyleAsKey;
    fontSize?: FontSize;
    fontWeight?: FontWeight;
    textAlign?: TextAlign;
    margin?: string;
    color?: Color;
    onClick?: () => void;
}
export interface TextPropsStyledProps {
    styleAs: SupportedStyleAsKey;
    fontSize: FontSize;
    fontWeight: FontWeight;
    color: Color;
    textAlign: TextAlign;
    margin?: string;
    onClick?: () => void;
}
export {};
