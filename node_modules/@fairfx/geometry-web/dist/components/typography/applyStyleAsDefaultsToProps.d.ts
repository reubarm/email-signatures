import { SupportedStyleAsKey, TextProps, TextPropsStyledProps } from './types';
declare type ApplyDefaultsProps = Omit<TextProps, 'as'> & {
    styleAs: SupportedStyleAsKey;
};
declare type ApplyStyleAsDefaultsToProps = (props: ApplyDefaultsProps) => Omit<TextPropsStyledProps, 'as'>;
declare const applyStyleAsDefaultsToProps: ApplyStyleAsDefaultsToProps;
export default applyStyleAsDefaultsToProps;
