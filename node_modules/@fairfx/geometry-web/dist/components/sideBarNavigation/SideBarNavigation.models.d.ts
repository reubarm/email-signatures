/// <reference types="react" />
import { Mode } from '../../typings/types';
export declare type SideBarNavigationProps = {
    /** @deprecated use primaryNavigation instead */
    children?: React.ReactElement | React.ReactElement[];
    secondaryNavigation?: React.ReactElement[];
    primaryNavigation?: React.ReactElement[];
    mode?: Mode;
    onCollapsing?: (value: boolean) => void;
    onLogoClick?: () => void | null;
    /** Defaults to the Equals Logo */
    expandedLogo?: React.ReactElement;
    /** Logo to show when the navigation is retracted. Defaults to the square equals logo. */
    retractedLogo?: React.ReactElement;
};
export interface SideBarNavigationRootProps {
    isToggled: boolean;
}
