import React from 'react';
import { Size } from '../../typings/types';
interface DropdownProps {
    text?: React.ReactNode;
    hideText?: Size;
    children: React.ReactNode;
}
declare const Dropdown: React.FC<DropdownProps>;
export default Dropdown;
