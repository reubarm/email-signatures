import { ReactNode } from 'react';
import { OptionsType, Props, Styles, ValueType } from 'react-select';
export declare type OptionType = {
    label: string | ReactNode | HTMLElement;
    value: string | number | object;
};
export declare type SelectValue = ValueType<OptionType>;
interface SelectProps extends Props<OptionType> {
    isError?: boolean;
    width?: string;
    testId?: string;
    styles?: Partial<Styles>;
    options?: OptionsType<OptionType> | any[];
}
declare const Select: (props: SelectProps) => JSX.Element;
export default Select;
