import { Theme } from '../../../theme/models';
export interface InputRootProps {
    isFocused: boolean;
    isFilled: boolean;
    isReadonly: boolean;
    isError: boolean;
    prefix?: string;
    theme?: Theme;
    isLocked: boolean;
}
