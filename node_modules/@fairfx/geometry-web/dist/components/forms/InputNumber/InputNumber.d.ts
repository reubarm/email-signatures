/// <reference types="react" />
import { InputTextProps } from '../input/Input';
export interface InputNumberProps extends InputTextProps {
    large?: boolean;
    currency?: string;
}
declare const InputNumber: ({ onChange, large, currency, value, ...rest }: InputNumberProps) => JSX.Element;
export default InputNumber;
