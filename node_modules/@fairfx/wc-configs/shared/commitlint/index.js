const scopes = require('../../src/helpers').getScopes();

module.exports = {
  extends: ['@commitlint/config-conventional'],
  parserPreset: require('./parser'),
  rules: {
    'scope-empty': [2, 'never'],
    'scope-enum': scopes,
    'subject-case': [2, 'always', ['sentence-case']],
  },
};
