const HEADER_REGEX = /^(\w*)\(([a-zA-Z-]*)\): (?:([A-Z]{2,}-[1-9]{1}\d*|=>|NO_JIRA))\s(.+)$/;

module.exports = {
  parserOpts: {
    headerPattern: HEADER_REGEX,
    headerCorrespondence: ['type', 'scope', 'ticket', 'subject'],
  },
};
