#!/usr/bin/env node
'use strict';

const bootstrap = require('commitizen/dist/cli/git-cz').bootstrap;
const rootPath = require('../../src/helpers').rootPath;

const czPath = `${rootPath}/node_modules/commitizen`;

bootstrap({
  cliPath: czPath,
  config: {
    path: '@commitlint/prompt',
  },
});
