"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const types_1 = require("./types");
function parseOptions(ruleArguments) {
    const optionSet = ruleArguments[0];
    return {
        importsGroups: parseImportsGroups(optionSet['imports-groups'])
    };
}
exports.parseOptions = parseOptions;
const parseImportsGroups = (importsGroups) => importsGroups.map(parseImportsGroupConfig);
function parseImportsGroupConfig(importsGroupConfig) {
    if (importsGroupConfig.type === types_1.ImportsGroupType.ThirdParty) {
        return {
            type: types_1.ImportsGroupType.ThirdParty
        };
    }
    return {
        type: types_1.ImportsGroupType.Project,
        regExp: new RegExp(importsGroupConfig.regExp)
    };
}
